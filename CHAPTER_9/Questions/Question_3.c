// Question_3.c -- several function headers.

#include <stdio.h>

char n_to_char(int n);
int digits(double a, int b);
int digits(double a, int b);
double * which(double *l, double *m);
int random(void);

int main(void){

	return 0;
}
